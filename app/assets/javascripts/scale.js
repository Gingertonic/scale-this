function Scale(attr){
  this.name = attr.name;
  this.scale_type = attr.scale_type;
  this.origin = attr.origin;
  this.melody_rules = attr.melody_rules;
  this.created_by = attr.created_by;
  this.private = attr.private;
  this.pattern = attr.pattern;
}
