<% if obj.errors.any? %>
  <div id="error_explanation">
    <h2>Oops, there's <%= pluralize(obj.errors.count, "error") %>!</h2>
    <ul>
      <% obj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
