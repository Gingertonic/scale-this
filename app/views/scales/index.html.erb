<h1 class="header">SCALE LIBRARY</h1>

<div class="container">

  <!-- <button type="button" id="dim" name="button">Diminish!</button> -->
  <!-- <div class="primary_content"> -->
  <!-- <div class="flash-error primary_error"></div> -->
    <%=content_tag :div, class: ["primary_content"] do%>
      <%@types.each do |type|%>
        <%=content_tag :div, class: ["scale_type"], id: ["#{type}"] do%>
          <h2><%=type%></h2>
          <%of_type(@scales, type).each do |s|%>
            <p><%=li_for_scale(s)%></p>
          <%end%> <!-- of_type.each -->
        <%end%> <!-- scale_type -->
      <%end%> <!-- types.each -->
    <%end%> <!-- scales_menu / primary content-->
  <!-- </div> -->

  <div class="sidebar">
    <div class="sb_nav">
      <%=content_tag(:button, link_to("Add a new scale!", new_scale_path), class: ["add_scale"])%>
    </div>
    <div class="sb_header">

    </div>
    <div class="sb_content">

    </div>
    <br></br>
    <div class="sb_errors"></div>
  </div>
</div> <!-- container -->
